package com.sagar.hr.util.enums;

public enum Status {

    // is active
    ACTIVE,
    // is in active
    INACTIVE,
    // is disabled not able to log into
    DISABLED,
    // is removed cannot be recovered
    REMOVED
}
